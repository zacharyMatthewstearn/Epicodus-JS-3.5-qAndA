import { posOrNeg } from 'q-and-a/helpers/pos-or-neg';
import { module, test } from 'qunit';

module('Unit | Helper | pos or neg');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = posOrNeg([42]);
  assert.ok(result);
});
